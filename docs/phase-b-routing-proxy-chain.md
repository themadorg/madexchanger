# Phase B — Routing, rewrite, proxy, multi-hop

## Goals
- Document and ops-test: routing rules, envelope rewrite, outbound SOCKS/HTTP proxies.
- Multi-hop chain examples (exchanger A → exchanger B → madmail).
- Optional: sample admin API scripts to seed routes/filters.

## Depends on
Phase A push–push stable.

## Status
Scaffold only — implement after Phase A green.
